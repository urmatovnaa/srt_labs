#include <iostream>
#include <string>

void runWarmup() {
    std::string s = "01234";
    std::cout << "Пункт 1.1 - Разминка:" << std::endl;

    // for(unsigned int i = s.size() - 1; i >= 0; i-- ) {
    //     std::cout << s[i] << std::endl;
    // }
}